import { CalloutDirective } from './callout.directive';

describe('CalloutDirective', () => {
  it('should create an instance', () => {
    const directive = new CalloutDirective();
    expect(directive).toBeTruthy();
  });
});
